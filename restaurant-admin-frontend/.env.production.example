# 正式部署前，請複製成 .env.production，並把網址改成實際後端 server。
# Vite 會在 npm run build 時讀取 VITE_ 開頭的變數。

# 一般 API 請求、直接跳轉到後端付款頁時使用。
VITE_API_BASE_URL=http://後端網址

# Vite dev server proxy 目標；若正式部署只使用 npm run build，這個通常不會用到，
# 但保留給同一份設定在開發模式部署時使用。
VITE_API_TARGET=http://後端網址
